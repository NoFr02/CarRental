import Logout from "../components/Account/logout";

/**
 * renders the Logoutform
 * @param {*} props 
 * @returns 
 */
const Logoutpage = (props) => {

    return (
        <div className='flex flex-col justify-center items-center'>
            <Logout />
        </div>
    )
}
export default Logoutpage;